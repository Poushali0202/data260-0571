# Required evidence

Save the real captures here:

- `local-app.png` — browser showing `http://localhost:8571`
- `local-console.png` — browser console showing a successful submission
- `ecs-public-ip.png` — browser showing the running ECS public IP
- `agents-demo.png` — Planner, Reviewer, and Finalized JSON output
- `client-stats.png` — `/stats` after turns 3 and 5 plus token totals

Do not replace these with fabricated images; capture them from the real runs.
