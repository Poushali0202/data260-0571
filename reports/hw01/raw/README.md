# Raw experiment outputs

Run `python run_nondeterminism.py --model qwen3:8b --runs-per-temperature 20`
from the repository root. The command writes the required 40-run JSON file
here. Keep the generated file in the tagged submission.
